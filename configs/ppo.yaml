# PPO config with tuned hyperparameters
algo: PPO
total_timesteps: 2000000
seed: 0
save_path: models/ppo.zip
log_dir: logs/ppo

n_envs: 8
eval_freq: 25000
eval_episodes: 5
checkpoint_freq: 50000

vec_norm: true
grayscale: true
resize: 84
frame_stack: 4
clip_action: true
